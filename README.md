# SGR Agent Core (.NET Port)

Это порт архитектуры **Schema-Guided Reasoning (SGR)** на платформу .NET 8.
Проект является C# реализацией логики автономного исследовательского агента, изначально представленной в репозитории [sgr-agent-core](https://github.com/vamplabAI/sgr-agent-core).

Главная особенность — использование строго типизированных схем (JSON) для управления мыслительным процессом агента, что позволяет избежать галлюцинаций и четко следовать циклу: **Reasoning (Рассуждение) -> Action (Действие) -> Observation (Наблюдение)**.

## 🚀 Особенности

*   **SGR Архитектура**: Агент сам решает, когда искать информацию, когда читать контент, а когда формировать ответ, возвращая строго валидированный JSON.
*   **Semantic Kernel**: Используется как ядро для взаимодействия с LLM.
*   **Устойчивость (Resilience)**: Встроенные механизмы повторных попыток (Retries) через Polly для сетевых запросов (например, если поисковый провайдер временно недоступен).
*   **Совместимость API**: Реализован специальный HTTP-хендлер (`StatusCodeFixHandler`), который позволяет работать с любыми OpenAI-совместимыми провайдерами, даже если они возвращают нестандартные статус-коды (например, 201 вместо 200).
*   **Deep Research**: Агент умеет выполнять поиск в интернете (Tavily) и вычитывать полное содержимое веб-страниц для глубокого анализа.

## ⚠️ Отличия от Python-версии

Этот проект реализует **Backend Core** логику.
*   ❌ **Нет UI**: В комплекте нет веб-интерфейса (Frontend), взаимодействие происходит через REST API.
*   ❌ **Нет Стриминга (SSE)**: Ответы не стримятся по токенам в реальном времени. Используется асинхронный запуск задач и поллинг статуса.
*   💾 **In-Memory хранилище**: История диалогов хранится в памяти. При перезапуске сервера данные сбрасываются (интерфейс репозитория позволяет легко подключить PostgreSQL/Redis).

## ⚙️ Настройка и Запуск

### 1. Предварительные требования
*   .NET 8 SDK
*   API ключ от OpenAI или любого совместимого провайдера (OpenRouter, local LLM и др.).
*   API ключ от [Tavily](https://tavily.com/) (для поиска в интернете).

### 2. Конфигурация
Перед запуском **обязательно** укажите свои ключи в файле `Sgr.DeepResearch.Api/appsettings.json`.

```json
{
  "AI": {
    "Endpoint": "https://api.openai.com/v1", // Или URL вашего провайдера
    "ApiKey": "YOUR_AI_API_KEY_HERE",        // ВАШ КЛЮЧ
    "ModelId": "gpt-4o-mini"                 // Рекомендуемая модель
  },
  "Search": {
    "TavilyApiKey": "tvly-YOUR_TAVILY_KEY"   // ВАШ КЛЮЧ TAVILY
  }
}